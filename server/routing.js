const express = require('express');
const router = express.Router();

// request functions
const {
    // exported names from controller file go here
} = require('./controllers/mainController');

// API CRUD operation routing
/*
router.post();
router.get();
router.patch();
router.delete();
*/

module.exports = router;